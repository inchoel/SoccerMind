# Secrets — never commit keys
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Runtime cache (TTL disk cache)
data_cache/

# 사용자별 보정 결과 (각자 calibrate 로 생성)
data_snapshots/model_config.json

# Claude Code local config
.claude/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
